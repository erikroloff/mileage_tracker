<!-- create a form that can be used by both the new and edit actions -->

<%= form_for(@make) do |f| %>
  <% if @make.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@make.errors.count, "error") %> prohibited this make from being saved:</h2>

      <ul>
      <% @make.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!-- fill in the rest of the form here -->
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit "Submit Name", class: "btn btn-large btn-primary" %>
<% end %>
